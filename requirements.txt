# Runtime dependencies (mirrors `[project.dependencies]` in pyproject.toml).
# This file is provided for convenience. Prefer installing via:
#   pip install -e .             # core
#   pip install -e .[examples]   # core + matplotlib
#   pip install -e .[nmpc]       # NMPC backends (casadi, do-mpc)
#   pip install -e .[gpu]        # JAX CUDA 12 wheels
#   pip install -e .[dev]        # development tooling

jax>=0.6.2
jaxlib>=0.6.2
equinox>=0.13.1
diffrax>=0.7.0
qpax>=0.0.9
mpax>=0.2.0
immutabledict>=4.2.1
numpy>=2.0
scipy>=1.13
